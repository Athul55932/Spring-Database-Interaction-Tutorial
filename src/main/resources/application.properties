spring.application.name=Spring-Tutorial-DBMS
spring.datasource.url=jdbc:mysql://localhost:3306/tutorial
spring.datasource.username=root
spring.datasource.password =9862987962
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto= validate
#the above line updates the database
# automatically when a new entity is changed.
# if we would use validate instead of update then it would stop
# starting the app whenever there is a change or mismatch in the database.

